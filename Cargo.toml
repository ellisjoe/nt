[package]
name = "nt"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.2", features = ["derive"] }
chrono = "0.4.45"
socket2 = { version = "0.6.5", features = ["all"] }

[dev-dependencies]
rstest = "0.26.1"